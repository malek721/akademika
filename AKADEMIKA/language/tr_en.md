# LANGUAGE MODULE
type: language
direction: Turkish → English
targets_core: 1.0

DIRECTIONAL RULES (Turkish → English)
  • Unpack agglutinative syntax into natural English.
  • Restore dropped subjects (Turkish pro-drop → English requires explicit subjects).
  • Lean active for journal venues where it does not alter emphasis (SUBORDINATE to
    CORE §4.5).
  • Eliminate Turklish calques: "make an analysis" → "analyze"; "realize a study"
    → "conduct a study".
  • "anlamlı" → "statistically significant" ONLY in statistical contexts; else
    "meaningful/significant" per sense.
  • "Bu çalışmada" → vary: "In this study" / "The present study".

locale_data:
  VARIANT: honor TARGET_VARIANT (US default). US: analyze, behavior, modeling.
    UK: analyse, behaviour, modelling. Never mix.
  CAPITALIZATION: English title/sentence case per venue.
  NUMBERS (per CORE §7, target = English): decimal → period; thousands → comma;
    percent sign AFTER the number (%92,5 → 92.5%).
  QUOTATION GLYPHS: “…” (US) consistently.

gender:
  Turkish "o" is genderless. In English, use the gender the SOURCE establishes
  elsewhere (name, role, explicit reference); if unspecified, use singular "they".
  NEVER guess a gender (CORE §12, Tier 1).

hedging_map (equal force — feeds CORE §11):
  kanıtlamaktadır / ortaya koymaktadır → demonstrates / establishes
  göstermektedir / saptamaktadır       → shows / finds
  işaret etmektedir / desteklemektedir → indicates / supports
  düşündürmektedir / görünmektedir     → suggests / appears to
  -Ebilir (olabilir …)                 → may / might / could
  muhtemelen / olası                   → is likely to / probably
  belki / tartışmalı olarak            → perhaps / arguably
  RULE: match force exactly; "göstermektedir" is "shows", not "proves".
