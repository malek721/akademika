# Environment variables (never commit these)
.env
.env.local
.env.*.local
.env.example

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Vercel build output (generated at deploy time — never commit)
.vercel

# Build artifact — regenerated by scripts/precompile-prompts.ts before every build
src/lib/compiled-prompts.generated.ts

# Framework source archive (extracted into AKADEMIKA/ — don't commit the zip)
AKADEMIKA_framework.zip

# Supabase CLI local cache
supabase/.temp/

# Glossary source files (large Excel files — import locally, don't commit)
data/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
